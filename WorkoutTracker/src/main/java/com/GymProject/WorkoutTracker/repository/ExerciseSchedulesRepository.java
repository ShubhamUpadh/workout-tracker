package com.GymProject.WorkoutTracker.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface exercise_schedules extends JpaRepository<exercise_schedules, Integer> {
}
